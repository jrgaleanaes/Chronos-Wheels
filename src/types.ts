export interface NavItem {
  label: string;
  href: string;
}

export interface Feature {
  title: string;
  description: string;
  icon: string;
}

export interface Review {
  author: string;
  role: string;
  content: string;
  rating: number;
}
